<h1>検索結果</h1>
<table>
  <head>
    <tr>
      <th>キャラクター名</th>
      <th>登場作品</th>
      <th colspan="4"></th>
    </tr>
  </head>

  <body>
    <% @results.each do |character| %>
      <tr>
        <td><%= character.chara_name %></td>
        <td><%= character.appear_in%></td>
        <td><%= link_to 'Show', character %></td>
        <td><%= link_to 'edit', character %></td>
      </tr>
    <% end %>
  </body>
</table>
<%= link_to '戻る', characters_path %>


<%= paginate @results%>