<% provide(:title, 'All characters') %>
<h1>All characters</h1>
推しは見つかりましたか？　見つからない場合はぜひ
<%= link_to 'キャラ登録',character_new_path %>
をお願いします。
<ul class="characters">
  <% @characters.each do |character| %>
    <li>
      <h4><%=character.chara_name %>
       <%= link_to "edit",edit_path(@character)%>
      <p></p>
      <%=character.appear_in %>
     
    </li>
  <% end %>
</ul>