<% if @character.errors.any? %>
    <div id="error_explanation">
        <div class = "alart alart-danger">
            The form contaians <%= pluralize(@character.errors.count, "error") %>
        </div>
        <ul>
        <% @character.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
<% end %>