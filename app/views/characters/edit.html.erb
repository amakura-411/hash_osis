<% provide(:title, '編集') %>
<h1>アップデート</h1>
<h2>基本情報</h2>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @character, local: true) do |f| %>
    <div class="field">
      キャラの名前<%= f.text_field :chara_name %>
      <p></p>
      登場作品<%= f.text_field :appear_in  %>
    </div>
    <%= f.submit "アップデート", class: "btn btn-primary" %>
    <% end %>

  </div>
</div>