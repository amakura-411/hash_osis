<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.find_each do |user| %>
    <li>
      <%= link_to user.username, user %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>